# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Ideas' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Ideas
  pod 'SDWebImage', '~> 5.0'
  pod 'UIGradient'

  target 'IdeasTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'IdeasUITests' do
    # Pods for testing
  end

end
